# Can I set the license key using WP CLI?

Not currently supported. The license key has to be entered through the plugin's settings screen in the WordPress admin.

We get why this comes up — CI/CD pipelines and multi-site rollouts both benefit from being able to set the key without clicking through the admin UI. It's on the radar for a future release, but there's no ETA yet.
